node_modules
dist
.DS_Store
*.local

# Third-party reference material (DKU CS Club blog archive etc.) — not part of the site
参考资料/

# Cloudflare
.wrangler/
cloudflare/seed.sql
cloudflare/.site-key.txt

# Working notes and local tooling state — kept on disk, not in the repository
.workbuddy/
